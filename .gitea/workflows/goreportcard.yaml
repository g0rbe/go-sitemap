name: Go
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     
      - name: Refresh Go Report Card
        run: /usr/bin/wget --quiet -O- --post-data='repo=git.gorbe.io/go/sitemap' 'https://goreportcard.com/checks'
